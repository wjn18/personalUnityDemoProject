using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StatsDebugInput : MonoBehaviour
{
   // public PlayerStatsRuntime stats;

   // void Awake()
   // {
      //  if (stats == null) stats = GetComponent<PlayerStatsRuntime>();
    //}

    //void Update()
    //{
        //if (Input.GetKeyDown(KeyCode.H)) stats.ApplyDamage(10);
       // if (Input.GetKeyDown(KeyCode.J)) stats.Heal(10);
        //if (Input.GetKeyDown(KeyCode.E)) stats.AddExp(25);
    //}
}